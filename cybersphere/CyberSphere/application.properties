server.port=8080
server.servlet.session.timeout=30m

spring.datasource.url=jdbc:mysql://localhost:3306/cybersphere?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=changeMe

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=10

spring.thymeleaf.cache=false

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

# Upload location (set to an existing writable directory)
app.upload.dir=static/uploads

# Session cookie settings (intentionally insecure options are applied in code per training requirement)

